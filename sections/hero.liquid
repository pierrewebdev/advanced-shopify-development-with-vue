<div class="hero-image section">
    <img src="{{ section.settings.image | img_url: "master" }}" alt="">
</div>

{% schema %}
{
    "name": "Hero Image",
    "settings": [
        {
            "type": "image_picker",
            "id": "image",
            "label": "image"
        }
    ],
    "presets": [
        {
            "name": "Hero Image",
            "category": "Image"
        }
    ]
}
{% endschema %}